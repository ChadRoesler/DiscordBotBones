namespace DiscordBotBones.Constants
{
    internal sealed class ResourceStrings
    {
        internal const string DateFormatString = "yyyy-MM-dd HH:mm";
        internal const string TimeToStringFormat = "yyyy-MM-dd HH:mm";
        internal const string UnknownCommand = "<unknown command>";
        internal const string NoMessage = "<no message>";
        internal const string AccessDeniedTitle = "Access Denied";
    }
}
